pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'sonar-scala'

include 'sonar-scala-plugin'
include 'its:plugin'
include 'its:ruling'
